; Roster-branch addition: runtime PHP settings for the phpMyAdmin container.
; Sized for importing/exporting the WeRoster database dump, not for a shared host.
memory_limit = 512M
upload_max_filesize = 512M
post_max_size = 512M
max_execution_time = 600
session.save_path = /sessions
